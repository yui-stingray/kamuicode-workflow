name: Media Synthesis

on:
  workflow_dispatch:
    inputs:
      concept:
        description: 'Media concept (e.g., "Cyberpunk city night, electronic")'
        required: true
        type: string
        default: 'Cyberpunk city night, electronic'

# 以下、同じ内容だが用語を変更：
# audio-generation → media-generation
# audio_strategy → media_strategy
# AUDIO_DIR → MEDIA_DIR
# audio/ → media/
# Audio → Media
