name: MV Gen
on:
  workflow_dispatch:
    inputs:
      concept:
        description: 'Music concept'
        required: true
        type: string
        default: 'Electronic music'
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Concept: ${{ github.event.inputs.concept }}"
